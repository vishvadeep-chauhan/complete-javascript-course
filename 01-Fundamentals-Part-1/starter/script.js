let js = "amazing";
console.log("vishvadeep");
console.log(23);

let firstName = "vishv";
console.log(firstName);

// let new = 27 ;
let vishv_deep = "nameofUser";
let $function = 26;

let person = "deep";

let PI = 3.2343;

let myFirstJob = "Programmer";

// -----------------------  data types --------------------
//
//  datatype defines what the kind of data , and variable storage of a value , javascript has two main types

// primitive and object

// primitive - stored by value
let name = "vishv";

// 1. number = floating point number used for decimals and integer
let age = 23;

// 2. string = sequence of characters , Used for text
let UserName = "vishvadeep";

//3. Boolean = Logical type that can only zero or one / true or false , used for taking decisions;
let UserAge24 = true;

//4. Undefined = Value taken by a variable that is not yet defined
let children;

// 5. Null = also mains "empty value"
//6. Symbol (es2015) = value that is unique and that is can not be changed . ( not useful for now )
//7. Bigint (es2020) = larger integer then the number can hold .

// javascript dynamic typing language , it can automatic determine datatype of a value

let javascriptIsTrue = true;
console.log(javascriptIsTrue);

console.log(typeof true);
